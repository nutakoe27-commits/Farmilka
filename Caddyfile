# Без домена (играть по IP, ws:// без шифрования):
:80 {
	reverse_proxy game:3000
}

# С доменом (автоматический HTTPS + wss:// от Let's Encrypt):
# замените example.com на свой домен, удалите блок :80 выше и раскомментируйте:
#
# example.com {
# 	reverse_proxy game:3000
# }
